name = "yyyt0807/external-sort"

version = "0.1.0"

readme = "README.mbt.md"

repository = "https://github.com/yyyt0807/MoonExternalSort"

license = "Apache-2.0"

keywords = [ "external-sort", "bounded-memory", "k-way-merge", "jsonl" ]

preferred_target = "wasm-gc"

description = "Stable bounded-memory external sorting and merge planning for MoonBit"

import {
  "moonbitlang/async@0.20.6",
}
