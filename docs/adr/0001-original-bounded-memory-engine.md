# Build an original bounded-memory sorting engine

MoonExternalSort is an original MoonBit implementation guided by published
external-sorting algorithms, not a source port. Its public value is the combined
contract of stable ordering, explicit resource budgets, bounded merge fan-in,
recoverable manifests, and atomic output rather than another in-memory sort.
